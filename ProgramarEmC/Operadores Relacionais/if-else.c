#include <stdio.h>

int main(){
    int preco1 = 76.0;
    int preco2 = 76.0;

    if(preco1 > preco2) {
        printf("Preco1 e maior que preco2");
    } else {
        if(preco1 < preco2)
            printf("Preco1 e menor que preco2");
        else(preco1 < preco2);
            printf("Os precos sao iguais");
    }
    
    return 0;
}